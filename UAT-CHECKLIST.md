# UltiEconomy — UAT Checklist

This document is the executable companion to `FEATURES.md`: one row per feature stating the
steps to exercise it and the observable truth that proves it works. It is an internal reference
for real-machine verification, not user-facing documentation.

> Batches are dispatched at 60 rows or fewer, and a batch never spans two repositories. There are
> exactly two legitimate exits to `human-uat-pending`: a row needing the pixel layer while the
> real-client harness is not ready, and a row needing personal credentials. Every other row must
> reach `pass`, `fail`, or `blocked`.

## Conventions

- **Columns:** `ID`, `Preconditions`, `Steps`, `Expected`, `Layer`, `Covers`.
- **ID:** cites its `FEATURES.md` ID verbatim. A negative case suffixes the checklist ID only, as
  `.neg-<slug>` — a negative case still tests the same feature, so the base ID is unchanged.
  A second row exercising a different non-failing path of the same feature suffixes `.<slug>`
  with no `neg-`, because calling a passing path negative would misreport what the row asserts
  (the same clause as the UltiKits checklist's). Two rows use it:
  `ultieconomy.scheduled.interest-payment.reload-interval` and
  `ultieconomy.scheduled.leaderboard-refresh.reload-interval`.
- **Layer**, copied verbatim from Laojun's own `ultitools-real-client-uat` skill: `protocol`,
  `java-client`, `os-input`, `pixel`, `server`, `human`. This module has no panel-facing surface,
  so no row below carries `human`.
- **Covers** back-references a Phase 9 GUI-excluded class name; left blank when no such class
  applies. UltiEconomy is not one of the nine modules in Phase 9's GUI-exclusion register (no
  `UltiEconomy.md` file exists under `.planning/phases/09-module-ecosystem-readiness-and-test-coverage/gui-exclusions/`),
  so every row below leaves `Covers` blank.
- A row whose Preconditions cite a prior row's checklist ID must appear after that row in file
  order — asserted mechanically: for every row, every checklist ID literally cited in its
  Preconditions cell must have a strictly smaller line number in this file than the row citing it
  (sweep class 8, D-27a). A Preconditions cell that merely *describes* a state to set up (e.g. "a
  second, non-primary currency configured") without citing another row's ID by name is not
  subject to this check.
- **Expected** must name an observable truth — an exact chat line, a log line, a database row —
  and never the words "it works". **This document is English-only (D-02) — a raw Chinese message
  literal is never reproduced inline.** Every chat and console line this module writes goes through
  its language catalogue with an ASCII key (wave 3, D-09/D-10; the 23 keys UltiKits/UltiEconomy#14
  found missing are in both catalogues), so a row quoting a line carries `language: en`. For a row
  whose English text was introduced or changed by wave 3, the session also moves
  `plugins/UltiTools/pluginConfig/UltiTools-Economy/lang/` aside before the start so the jar's
  catalogues are extracted fresh (an upgraded install never refreshes an extracted language file,
  UltiKits/UltiTools-Reborn#459).
- **Multi-currency precondition:** this module ships exactly one currency by default
  (`config/currencies.yml`'s single `coins` entry, marked `primary: true`). Every row below whose
  ID ends `-currency` needs a SECOND, non-primary currency added to `config/currencies.yml` before
  it can be exercised at all — stated in that row's own Preconditions, not cited from a shared
  row, since adding a currency is a file edit, not a feature this document gives its own
  checklist row (see `## Configuration` below for why).
- **Config-per-file rule (D-06), narrowed for this module's own layout:** UltiEconomy ships two
  yml resources — `config/config.yml` (the module's one `@ConfigEntity`-bound file) and
  `config/currencies.yml` (read directly via `YamlConfiguration`, not `@ConfigEntity`-bound). Per
  this plan's own explicit instruction ("exactly one config row for the single `@ConfigEntity`
  class"), this document carries exactly ONE config-per-file row, `ultieconomy.config.config-yml`,
  whose Steps exercise BOTH shipped files in the same pass — they load during the same server
  startup with no interdependent ordering, and `currencies.yml`'s own single row in `FEATURES.md`
  (`ultieconomy.config.currencies.currencies`) has no command surface of its own to test
  independently; the only way to exercise it at all is by editing the file and observing a
  currency-aware command, which the config-per-file row already does.

## Bank

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.bank.balance | `language: en` in config.yml; `bank.enabled: true` (shipped default); the sender has a nonzero bank balance (deposit first, or use a fresh account's `0.00`) | Run `/bank` | The chat line reads `Your bank balance: ` followed by the currency symbol and the amount formatted `#,##0.00` (e.g. `Your bank balance: $0.00`), in yellow; before wave 3 it rendered the untranslated Chinese source text instead (UltiKits/UltiEconomy#14) | server | |
| ultieconomy.bank.balance.neg-disabled | `language: en` in config.yml; `bank.enabled: false` in config.yml | Run `/bank` | Chat shows the red line `Bank feature is not enabled` (`economy.error.bank_disabled`, sent at `BankCommand.java:60`) | server | |
| ultieconomy.bank.balance-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) added to `config/currencies.yml`, server restarted after the edit | Run `/bank gems` | Same `Your bank balance: ...` line as `ultieconomy.bank.balance` above, formatted with `gems`'s own symbol, NOT the primary currency's `$` | server | |
| ultieconomy.bank.balance-currency.neg-unknown | `language: en` in config.yml | Run `/bank does-not-exist` | Chat line is the RED-colored message at `BankCommand.java:74` — this key DOES localize; under `language: en` it reads `Currency not found` | server | |

## Deposit

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.deposit.deposit | `language: en` in config.yml; `bank.enabled: true`; sender's cash ≥ 500 (not merely ≥ `bank.min-deposit`, since a cash balance between the 100.0 minimum and 500 would refuse with `Insufficient balance` before this row ever observes a success); `bank.max-balance` (default `-1`, unlimited) either left at `-1` or, if set positive, with at least 500 of remaining capacity above the sender's current bank balance (otherwise `depositToBank` refuses with `Bank balance has reached the limit` instead of succeeding) | Run `/deposit 500` | Chat line reads `Successfully deposited $500.00 to bank` (green); `/bank` immediately afterward shows the increased balance | server | |
| ultieconomy.deposit.deposit.neg-below-minimum | `language: en` in config.yml; `bank.enabled: true`; `bank.min-deposit: 100.0` (shipped default); `bank.max-balance` unlimited or with enough remaining capacity that the 50 requested below would not ALSO trip the limit — this row's whole point is isolating the minimum-deposit refusal, not accidentally observing the limit refusal instead | Run `/deposit 50` | Chat line is the RED-colored message at `DepositCommand.java:52` — this key DOES localize; under `language: en` it reads `Minimum deposit: $100.00` | server | |
| ultieconomy.deposit.deposit.neg-insufficient-cash | `language: en` in config.yml; `bank.enabled: true`; sender's cash < the amount requested but ≥ `bank.min-deposit` | Run `/deposit <amount greater than current cash>` | Chat line is the RED-colored "insufficient balance" message (`DepositCommand.java:67`) — localizes correctly to `Insufficient balance` under `language: en` | server | |
| ultieconomy.deposit.deposit.neg-max-balance | `language: en` in config.yml; `bank.enabled: true`; `bank.max-balance` set to a small positive value (NOT the shipped `-1` unlimited default) with the sender's current bank balance already at or near it | Run `/deposit <amount that would exceed bank.max-balance>` | Chat line is the RED-colored message at `DepositCommand.java:65` — localizes correctly to `Bank balance has reached the limit` | server | |
| ultieconomy.deposit.deposit-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured with `bank-enabled: true`, `min-deposit` set to 200 OR LESS, and `max-bank-balance` either `-1` (unlimited) or at least 200 above the sender's current `gems` bank balance; sender's `gems` CASH balance specifically ≥ 200 (not merely ≥ `min-deposit` — a cash balance between `min-deposit` and 200 would refuse with `Insufficient balance` before this row ever observes a success) | Run `/deposit 200 gems` | Chat line reads `Successfully deposited <amount> to bank`, formatted in `gems` | server | |
| ultieconomy.deposit.deposit-currency.neg-bank-globally-disabled | `language: en` in config.yml; `bank.enabled: false` in config.yml (the GLOBAL switch); a second currency (e.g. `gems`) configured with its OWN `bank-enabled: true`, `min-deposit` set to 200 or less, and `max-bank-balance` either `-1` or at least 200 above the sender's current `gems` bank balance; sender's `gems` cash ≥ 200 — the same three constraints as the row above, restated here rather than inherited, since this row's whole point is that `bank.enabled` makes no difference and every OTHER gate must therefore already be satisfied for the success to be observable | Run `/deposit 200 gems` | The deposit SUCCEEDS (the same `Successfully deposited <amount> to bank` line as the row above) despite the global `bank.enabled` switch being off — `DepositCommand#onDepositCurrency` never calls `EconomyConfig#isBankEnabled()` at all, unlike its primary-currency sibling exercised by `ultieconomy.deposit.deposit.neg-below-minimum` above; this divergent-path row exists specifically because the two mappings are NOT symmetrically gated | server | |

## Withdraw

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.withdraw.withdraw | `language: en` in config.yml; `bank.enabled: true`; sender's bank balance ≥ the amount requested (deposit first if needed) | Run `/withdraw 100` | Chat line reads `Successfully withdrew $100.00 from bank` (green); `/bank` immediately afterward shows the decreased bank balance and `/money` shows the increased cash | server | |
| ultieconomy.withdraw.withdraw.neg-insufficient | `language: en` in config.yml; `bank.enabled: true`; sender's bank balance < the amount requested | Run `/withdraw <amount greater than current bank balance>` | Chat line is the RED-colored message at `WithdrawCommand.java:55` — localizes correctly to `Insufficient bank balance` | server | |
| ultieconomy.withdraw.withdraw-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured, sender's `gems` bank balance ≥ the amount requested | Run `/withdraw 50 gems` | Chat line reads `Successfully withdrew <amount> from bank`, formatted in `gems`; no `bank.enabled` (global) gate applies to this mapping | server | |

## Pay

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.pay.pay | `language: en` in config.yml; `tax.transaction-tax.enabled: false` (NOT the shipped default — isolates this row from the gross-versus-net tax figure; see `.neg-tax-message-mismatch` below for the tax-and-message interaction); sender's cash ≥ the amount; a second online player as the target, not the sender | Run `/pay <target> 100` | The sender's line reads `Successfully transferred $100.00 to <target>` and the target's line reads `<sender> transferred $100.00 to you`, both green and both English under `language: en` (before wave 3 both rendered the untranslated Chinese source text, UltiKits/UltiEconomy#14), both quoting `100` (with tax disabled, the target's cash genuinely rises by exactly 100, so the figure and the actual credit agree here) | server | |
| ultieconomy.pay.pay.neg-tax-message-mismatch | `language: en` in config.yml; `tax.enabled: true` in the on-disk file (a file written by 2.0.0 holds `false` unless edited, which takes no tax at all; UltiKits/UltiEconomy#16); `tax.transaction-tax.enabled: true` (shipped default) with a nonzero `tax.transaction-tax.rate`; sender's cash ≥ the amount; a second online player as the target, not the sender | Run `/pay <target> 100`, then separately read the target's own `/money` cash figure before and after | BOTH chat lines (sender's and target's) quote the GROSS `100`, not the net amount actually credited — `PayCommand#onPay` formats `EconomyServiceImpl#transfer`'s own `amount` parameter (`PayCommand.java:60`), never the `received = amount - tax` value `transfer()` computes internally (`EconomyServiceImpl.java:210`) and actually applies to the target's balance; the target's `/money` cash figure rises by ONLY `100 - tax` (e.g. 95 at the shipped 5% rate), confirming the chat text overstates what was actually credited by the tax amount — UltiKits/UltiEconomy#18 | server | |
| ultieconomy.pay.pay.neg-tax-master-off | `language: en` in config.yml; in the on-disk `config.yml`, `tax.enabled: false` together with `tax.transaction-tax.enabled: true` and `tax.transaction-tax.rate: 0.05`; sender's primary-currency cash ≥ 200; a second online player as the target, not the sender | 1. Restart the server and read this boot's server log. 2. Note the target's `/money` cash figure and the primary currency's line in `/eco treasury` (console). 3. Run `/pay <target> 100`. 4. Read the target's `/money` cash and `/eco treasury` again. 5. Set `tax.enabled: true` in the same file, run `/ul reload UltiTools-Economy` from console, then repeat steps 3-4 | Step 1: exactly one WARN line containing `UltiEconomy: tax.enabled is false in config/config.yml, so no tax is collected at all`. Step 4: the target's cash rose by exactly `100.00` (not `95.00`) and the treasury line is unchanged. Step 5 (control, proving the instrument can see a tax and that the switch is read at the next transfer without a restart): the target's cash rises by exactly `95.00` and the treasury line rises by exactly `5.00` (UltiKits/UltiEconomy#16) | server | |
| ultieconomy.pay.pay.neg-self | `language: en` in config.yml | Run `/pay <own name> 10` | Chat shows the red line `You cannot pay yourself` (`economy.pay.self`, sent at `PayCommand.java:54`), and the player's balance is unchanged | server | |
| ultieconomy.pay.pay.neg-insufficient | `language: en` in config.yml; sender's cash < the amount; a second online player as the target | Run `/pay <target> <amount greater than sender's cash>` | Chat line is the RED-colored "insufficient balance" message (`PayCommand.java:66`) — localizes correctly to `Insufficient balance`; the target receives NO message at all (the transfer never reaches `EconomyServiceImpl#transfer`, which refuses before crediting anyone) | server | |
| ultieconomy.pay.pay.neg-offline-target | `language: en` in config.yml | Run `/pay <a name with no player currently online>` | Chat line is the RED-colored "player not found" message (`PayCommand.java:49`) — localizes correctly to `Player not found`; `Bukkit#getPlayer` only resolves ONLINE players for this command, unlike `/eco`'s admin commands which also accept offline targets | server | |
| ultieconomy.pay.pay-currency | `language: en` in config.yml; `tax.transaction-tax.enabled: false` (same isolation as `ultieconomy.pay.pay` above, for the same reason); a second, non-primary currency (e.g. `gems`) configured; sender's `gems` cash ≥ the amount; a second online player as the target | Run `/pay <target> 50 gems` | Both parties' messages are the same English lines as `ultieconomy.pay.pay` above (`Successfully transferred <amount> to <target>`, `<sender> transferred <amount> to you`), formatted in `gems`; the same gross-vs-net mismatch (UltiKits/UltiEconomy#18) applies to this currency-aware overload too if tax is re-enabled — `EconomyServiceImpl#transfer(from, to, amount, currencyId)` computes the identical `received = amount - tax` internally | server | |

## Money (balance display)

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.money.balance | `language: en` in config.yml | Run `/money` (also confirm `/bal` produces byte-identical output) | The four lines read `=== Economy System ===`, `Your balance: <cash>`, `Your bank balance: <bank>` and `Total wealth: <cash + bank>` (amounts formatted `$#,##0.00`), all English; before wave 3 the three body lines rendered the untranslated Chinese source text under the English header (UltiKits/UltiEconomy#14) | server | |
| ultieconomy.money.balance-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured | Run `/money gems` | Header reads `=== Economy System (gems) ===`; the three body lines read `Your balance: ...`, `Your bank balance: ...` and `Total wealth: ...`, formatted in `gems` | server | |
| ultieconomy.money.balance-currency.neg-unknown | `language: en` in config.yml | Run `/money does-not-exist` | Chat line is the RED-colored "currency not found" message (`MoneyCommand.java:74`) — localizes correctly to `Currency not found` | server | |

## Money Notes

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.note.create | `language: en` in config.yml; sender's primary-currency cash ≥ the amount requested; sender has at least one free inventory slot | Run `/note 500` | Chat line reads `Money note created: $500.00`; a `Material.PAPER` item is added to the sender's inventory whose display name reads `[Money Note] 500.00 coins` and whose lore reads `Currency: coins`, `Value: 500.00`, `Created by: <sender>` (the name and lore follow `language` since wave 3; the note is recognised by its stored data, not by this text); `/money`'s cash figure drops by 500 | server | |
| ultieconomy.note.create.neg-insufficient | `language: en` in config.yml; sender's cash < the amount requested | Run `/note <amount greater than current cash>` | Chat line is the RED-colored "insufficient balance" message (`NoteCommand.java:66`) — localizes correctly; no item is added to the inventory | server | |
| ultieconomy.note.create-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured; sender's `gems` cash ≥ the amount; sender has at least one free inventory slot — `NoteCommand#onCreateCurrencyNote` deducts the cash and calls `Inventory#addItem` without handling a full-inventory's returned leftovers, so a full inventory would deduct the cash with no note actually placed | Run `/note 20 gems` | Chat line reads `Money note created: <formatted>` (localizes correctly, `NoteCommand.java:100`); the created note's lore names `gems`, not the primary currency | server | |
| ultieconomy.note.redeem | `language: en` in config.yml; the sender is holding a money-note item in their main hand (created via `ultieconomy.note.create` above) | Run `/note redeem` | Chat line reads `Money note redeemed: $500.00` (localizes correctly, `NoteCommand.java:129`); the note item is consumed by one (removed entirely if the stack was 1); `/money`'s cash figure increases by the note's face value in its own currency | server | |
| ultieconomy.note.redeem.neg-empty-hand | `language: en` in config.yml; the sender is NOT holding a money-note item in their main hand | Run `/note redeem` | Chat line is the RED-colored message at `NoteCommand.java:108` — localizes correctly to `You are not holding a money note`; no balance change occurs | server | |
| ultieconomy.note.redeem-onuse | The sender is holding a money-note item in their main hand (created via `ultieconomy.note.create`) | Right-click air or a block while holding the note | The note is redeemed exactly as `ultieconomy.note.redeem` above (same chat line, same balance credit, same stack decrement, `NoteRedeemListener.java:75`) WITHOUT running any command; the interaction event that triggered it is cancelled regardless of outcome (confirmed by the block not being interacted with if right-clicking a block with its own interaction, e.g. a door) | os-input | |
| ultieconomy.note.redeem-onuse.neg-empty-hand | The sender is NOT holding a money-note item | Right-click air while holding an ordinary item (or empty hand) | Nothing happens — no chat line, no balance change, the interaction event is NOT cancelled by this listener (`NoteRedeemListener#onInteract`'s `isMoneyNote` check at line 54 returns false before any cancellation logic runs) | os-input | |

## Admin

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.admin.check | `language: en` in config.yml; a target player who has joined at least once before (online or offline) | Run `/eco check <target>` from console | Header line `=== <target> ===` (the player's name only, not translated); then `<target>'s balance: <cash>`, `<target>'s bank balance: <bank>` and `Total wealth: <total>`, all English (UltiKits/UltiEconomy#14) | server | |
| ultieconomy.admin.check.neg-unknown-player | `language: en` in config.yml | Run `/eco check does-not-exist-and-never-played` from console | Chat line is the RED-colored "player not found" message (`EcoAdminCommand.java:362`) — localizes correctly to `Player not found` | server | |
| ultieconomy.admin.check-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured; a target player who has joined before | Run `/eco check <target> gems` from console | Header line `=== <target> (gems) ===`; the three body lines are the same English lines as `ultieconomy.admin.check`, formatted in `gems` | server | |
| ultieconomy.admin.give | `language: en` in config.yml; a target player who has joined before, currently offline | Run `/eco give <target> 1000` from console | Chat line reads `Gave <target> $1,000.00` (green); `/eco check <target>` immediately afterward reflects the increased balance, proving the offline-target write actually persisted | server | |
| ultieconomy.admin.give-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured; a target player who has joined AT LEAST ONCE AFTER `gems` was added to `config/currencies.yml` — a player who last joined before `gems` existed has no `CurrencyBalanceEntity` row for it at all (those are created by `PlayerJoinListener` only on join), and `EconomyServiceImpl#addCash(..., "gems")` returns `false` for a missing balance row rather than creating one, which this row would otherwise misread as the command failing | Run `/eco give <target> 500 gems` from console | Chat line reads `Gave <target> <amount>`, formatted in `gems` | server | |
| ultieconomy.admin.take | `language: en` in config.yml; a target player whose current primary-currency cash ≥ the amount | Run `/eco take <target> 200` from console | Chat line reads `Took $200.00 from <target>` (green) | server | |
| ultieconomy.admin.take.neg-insufficient | `language: en` in config.yml; a target player whose current cash < the amount | Run `/eco take <target> <amount greater than current cash>` from console | Chat line is the RED-colored "insufficient balance" message (`EcoAdminCommand.java:117`) — localizes correctly | server | |
| ultieconomy.admin.take-currency | `language: en` in config.yml; a second, non-primary currency configured; a target player with sufficient `gems` cash | Run `/eco take <target> 50 gems` from console | Chat line reads `Took <amount> from <target>`, formatted in `gems` | server | |
| ultieconomy.admin.set | `language: en` in config.yml; a target player who has joined before | Run `/eco set <target> 9999` from console | Chat line reads `Set <target>'s balance to $9,999.00` (green); `/eco check <target>` afterward confirms the balance is now exactly 9999, not additive | server | |
| ultieconomy.admin.set-currency | `language: en` in config.yml; a second, non-primary currency (e.g. `gems`) configured; a target player who has joined AT LEAST ONCE AFTER `gems` was configured — the same requirement as `ultieconomy.admin.give-currency` above, for the identical reason: `EconomyServiceImpl#setCash(..., "gems")` returns `false` for a target with no `CurrencyBalanceEntity` row for `gems`, which `PlayerJoinListener` only creates on join | Run `/eco set <target> 42 gems` from console | Chat line reads `Set <target>'s balance to <amount>`, formatted in `gems` | server | |
| ultieconomy.admin.treasury | `tax.enabled: true` in the on-disk file and `tax.transaction-tax.enabled: true` (shipped default); at least one taxed transfer already collected via `ultieconomy.pay.pay.neg-tax-message-mismatch` above — NOT `ultieconomy.pay.pay`, which this document's own Preconditions deliberately disables tax for | Run `/eco treasury` from console | One line per configured currency reading `<display-name>: <formatted treasury balance>` (`EcoAdminCommand.java:290-293`) — this row's own messages localize correctly, since `EcoAdminCommand#onTreasury` never routes any of its per-currency output through `plugin.i18n(...)` at all | server | |
| ultieconomy.admin.treasury-withdraw | `language: en` in config.yml; the primary-currency treasury balance ≥ the amount requested — collect tax first via `ultieconomy.pay.pay.neg-tax-message-mismatch` above, NOT `ultieconomy.pay.pay`, which this document's own Preconditions deliberately disables tax for and would leave the treasury at zero | Run `/eco treasury withdraw <amount>` from console | Chat line reads `Withdrew $<amount> from treasury` (`EcoAdminCommand.java:312-313`, localizes correctly); `/eco treasury` afterward shows the reduced treasury balance | server | |
| ultieconomy.admin.treasury-withdraw.neg-insufficient | `language: en` in config.yml; the treasury's primary-currency balance < the amount requested | Run `/eco treasury withdraw <amount greater than current treasury balance>` from console | Chat line reads `Insufficient treasury balance` (`EcoAdminCommand.java:315`, localizes correctly) | server | |
| ultieconomy.admin.treasury-withdraw-currency | `language: en` in config.yml; a second, non-primary currency configured with a nonzero treasury balance | Run `/eco treasury withdraw <amount> gems` from console | Chat line reads `Withdrew <formatted> from treasury` (`EcoAdminCommand.java:336-337`, localizes correctly), formatted in `gems` | server | |

## Vault and PlaceholderAPI registration

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.vault.register | A Vault-compatible plugin installed alongside this module (e.g. a shop plugin, or Vault's own economy test command if available); Vault itself is a HARD dependency of this module (`plugin.yml: depend: [Vault]`), so this row cannot be exercised without Vault present at all | Query the registered `Economy` service (e.g. via a Vault-aware plugin's own economy check, or `/eco give`/`/money` interacting with a shop plugin that charges via Vault) | The Vault-compatible plugin reads and modifies the SAME cash balance `/money` reports — confirming `VaultEconomyProvider` is registered and correctly delegates to `EconomyServiceImpl` | server | |
| ultieconomy.placeholder.register.neg-absent | PlaceholderAPI NOT installed | Restart the server with this module loaded and PlaceholderAPI absent | The server starts with no error related to `EconomyPlaceholderExpansion` — `UltiEconomy#registerSelf` checks `Bukkit.getPluginManager().getPlugin("PlaceholderAPI") != null` before attempting registration, so its absence is a no-op, not a load failure | server | |
| ultieconomy.placeholder.register | PlaceholderAPI installed and enabled | Restart the server with this module loaded and PlaceholderAPI present, then run `/papi parse me %ultieconomy_cash%` (or equivalent) | The placeholder resolves to a raw two-decimal number rather than a literal `%ultieconomy_cash%` echo — confirming the expansion registered | server | |
| ultieconomy.vault.unregister | `ultieconomy.vault.register` above passed; Vault 1.7.3 installed (the test server's `Vault.jar` is `1.7.3-b131`) and NO other Vault economy plugin installed; `language: en` in the framework's own `plugins/UltiTools/config.yml`; every entry in `plugins/UltiTools/plugins/` is a `.jar` containing a `plugin.yml`, and exactly one of them has `name: UltiTools-Economy` (`PluginInstallUtils#uninstallPlugin` opens every entry's `plugin.yml` in `File#listFiles()` order, which is unspecified, so any other entry can make it throw before it reaches this module's jar and print `Delete Failed! File access error! Please delete it manually!` instead, and a second jar with the same name can be the one left behind); this module's jar copied from `plugins/UltiTools/plugins/` to a backup location outside the server's `plugins/` tree; `/vault-info` run from console reads `[Vault] Economy: UltiEconomy [UltiEconomy]` — if it does not, this row is `blocked`, not `pass` | 1. From console, run `/upm uninstall UltiTools-Economy` — `UltiTools-Economy` is this module's runtime name (its `plugin.yml` `name:`, which `uninstallPlugin` matches against); `/upm uninstall UltiEconomy` matches no module and only prints `Uninstall Failed! Please check if the spelling is correct!`. 2. Without restarting or reloading anything, run `/vault-info` from console. 3. Cleanup: copy the backed-up jar back into `plugins/UltiTools/plugins/` (a successful uninstall deletes it from there), restart the server (`/ul reload` does not load jars), then run `/vault-info` from console again | Step 1: console reply `Uninstalled! Please delete local files manually, otherwise it will be enabled after restart!` followed by `File Location: <server path>/plugins/UltiTools/plugins`, and this module's jar is no longer in that folder. Step 2: `/vault-info`'s economy line reads exactly `[Vault] Economy: None [null]` — Vault 1.7.3's `Vault#infoCommand` prints `None` for the active provider and `null` for the registered-provider list when no `Economy` service remains (read from the jar's bytecode) — and the text `UltiEconomy` appears nowhere in that line. A line still naming `UltiEconomy` is a `fail`: the provider outlived the module, so `UltiEconomy#onUnregister` did not deregister it (UltiKits/UltiEconomy#22). Step 3: after the restart the economy line again reads `[Vault] Economy: UltiEconomy [UltiEconomy]`, so later rows run against a restored module | server | |

## Player join

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.player.join-init | A player UUID that has never joined this server (with this module installed) before; at least one non-primary currency configured (e.g. `gems`, added before this join) | The player joins for the first time | `/money` immediately shows the shipped `initial-cash` value (1000.0 by default) as the player's primary-currency cash; `/money gems` shows `gems`'s own configured `initial-cash` (independently — a currency-specific starting balance, not the primary currency's) | server | |

## Scheduled tasks

These rows read the boot log's `Registered config-bound @Scheduled task: …` INFO line, which the
framework's `TaskManager` prints once per task it registers, and count it — a second copy of a task
after `/ul reload` is the failure these rows exist to catch, because a second interest task pays
twice. Both intervals are config keys (`interest.interval`, `leaderboard.update-interval`, in
seconds) bound through UltiTools 6.3.0 (UltiKits/UltiTools-Reborn#531); a reload that changes one
logs `rescheduled config-bound @Scheduled task …` instead of registering anything.
Balances are read with `/eco check <player>` from console (`language: en`: its body lines read
`<player>'s balance: …`, `<player>'s bank balance: …` and `Total wealth: …`). "A's bank reads X"
below means the bank line — the second body line — ends with the formatted amount X, which carries the currency symbol and thousands separators
(`$10,000.00`).

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.scheduled.interest-payment | `language: en` in config.yml; in the on-disk `config.yml`, `interest.enabled: true`, `interest.rate: 0.03`, `interest.interval: 1800`, `interest.max-interest: 10000.0`, `bank.enabled: true`, `bank.max-balance: 100000`; a second player B who has joined before, whose primary bank balance reads exactly `$99,900.00` in `/eco check B` and whose `coins` per-currency bank balance is `$0.00`; a player A who has joined before, whose primary bank balance reads exactly `$10,000.00` in `/eco check A` (e.g. `/eco give A 10000`, then A runs `/deposit 10000` on a fresh account) and whose `coins` per-currency bank balance reads exactly `$5,000.00` in `/eco check A coins` (e.g. `/eco give A 5000 coins`, then A runs `/deposit 5000 coins`) | 1. Restart the server, note the time T at which the server's `Done (` startup line is logged (the tick clock that times the payment starts with the server's first tick, which comes after that line, not at the earlier `UltiTools-Economy loaded` line), and read this boot's log. 2. At T+10 minutes, from console run `/ul reload UltiTools-Economy` twice, then read the log again (a third of the way into the first interval, so a reload that re-armed the task from the reload would move the first payment to T+40, past both judging times in step 4). 3. Keep A online. At T+29 minutes run `/eco check A`. 4. At T+31 minutes run `/eco check A`, `/eco check A coins` and `/eco check B` again (if the server ran below 20 TPS, repeat at T+35 before judging), and read A's chat since T | Step 1: exactly one line containing `Registered config-bound @Scheduled task: InterestService.payInterestIfEnabled (delay=36000, period=36000, async=false, config=EconomyConfig, periodKey=interest.interval=1800s, delayKey=interest.interval=1800s)`, and exactly one WARN line containing `UltiEconomy: interest.enabled is true in config/config.yml, so interest is paid: every interest.interval = 1800 seconds` that also contains `interest.rate = 0.03`, `interest.max-interest = 10000.0` and `If several servers share this database`. Step 2: no further `Registered config-bound @Scheduled task: InterestService` line, and no `rescheduled config-bound @Scheduled task` line (the value did not change). Step 3: A's bank still reads `$10,000.00` — nothing is paid at load. Step 4: A's bank reads exactly `$10,300.00` — one payment of `300.00`; `$10,600.00` would mean the reload registered a second task, and `$10,000.00` at both T+31 and T+35 would mean a reload re-armed the task from the reload although the value did not change — and A received exactly one interest chat line since T: `Bank interest received: $300.00`. `/eco check A coins` still shows a bank of exactly `$5,000.00` — the primary currency is paid once, on the account row, never also on its per-currency row (maintainer ruling 2026-09-23, UltiKits/UltiEconomy#25). B's bank reads exactly `$100,000.00` — interest stopped at `bank.max-balance` (`$102,897.00` would mean the cap was ignored) (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.scheduled.interest-payment.neg-disabled | Same player A with a primary bank balance reading exactly `$10,000.00` in `/eco check A` (any `coins` per-currency bank balance — it never earns interest); in the on-disk `config.yml`, `interest.enabled: false` and the other `interest.*` values as in the row above | 1. Restart the server, note the time T of the `Done (` startup line, and read this boot's log. 2. Keep A online; at T+31 minutes run `/eco check A`. 3. At T+35 minutes set `interest.enabled: true` in the same file, run `/ul reload UltiTools-Economy` from console (no restart), and read the log. 4. At T+61 minutes (the second 30-minute slot) run `/eco check A` (below 20 TPS, repeat at T+64 before judging) | Step 1: exactly one line containing `Registered config-bound @Scheduled task: InterestService.payInterestIfEnabled` — the task exists with the switch off — and no line containing `interest.enabled is true`. Step 2: A's bank still reads exactly `$10,000.00` and A received no interest chat line. Step 3: no `rescheduled config-bound @Scheduled task` line — only `interest.enabled` changed, and it is read at each run, not bound to the timer. Step 4 (control, proving the switch is read at each run without rescheduling, and that the instrument can see a payment): A's bank reads exactly `$10,300.00` — paid in the T+60 slot. `$10,000.00` at both T+61 and T+64 would mean the reload re-armed the task from the reload (next payment at T+65) (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.scheduled.interest-payment.reload-interval | `ultieconomy.scheduled.interest-payment` above passed; in the on-disk `config.yml`, `interest.enabled: true`, `interest.rate: 0.03`, `interest.interval: 1800`, `bank.max-balance: -1`; player A online with a primary bank balance reading exactly `$10,000.00` in `/eco check A` | 1. Restart the server and note the time T at which the server's `Done (` startup line is logged (the tick clock that times the payment starts with the server's first tick, which comes after that line, not at the earlier `UltiTools-Economy loaded` line). 2. At T+5 minutes set `interest.interval: 600` in the same file, run `/ul reload UltiTools-Economy` from console, and read the log. 3. At T+9 minutes run `/eco check A`. 4. At T+11 minutes run `/eco check A` again (below 20 TPS, repeat at T+13 before judging) | Step 2: exactly one line containing `UltiTools-Economy: rescheduled config-bound @Scheduled task InterestService.payInterestIfEnabled after reload (delay=12000, period=12000, async=false); next run in` followed by a number of ticks from 4800 to 7200 — the new 12000-tick first delay minus the ticks the server has run since load, so the first payment comes at the load plus the new 10-minute interval. It is about 6000 for a reload at T+5:00 at a steady 20 TPS; a later reload gives less, a server below 20 TPS more. The reload lands a sixth of the way into the first delay, so the two behaviours this separates differ by about 6000 ticks. Failure values: exactly `12000` means the reload restarted the clock from the reload; `1` means it ran the payment at once; no line means the new value was not applied. There is also no new `Registered config-bound @Scheduled task` line. Step 3: A's bank still reads `$10,000.00` — the reload did not pay early. Step 4: A's bank reads exactly `$10,300.00` — the first payment came at T+10 minutes, the load plus the new interval, not at T+15 (reload plus interval, which would mean the reload postponed it) and not at T+30 (the old interval) (UltiKits/UltiEconomy#15, UltiKits/UltiTools-Reborn#531) | server | |
| ultieconomy.scheduled.interest-payment.neg-invalid-interval | `ultieconomy.scheduled.interest-payment` above passed | 1. Set `interest.interval: 0` in the on-disk `config.yml`, restart, read the log, and run `/money` as a player. 2. Set `interest.interval: 1800`, restart, and read the log. 3. Set `interest.interval: 0` again, run `/ul reload UltiTools-Economy` from console (no restart), and read the log. 4. Restore: set `interest.interval: 1800` in the file, run `/ul reload UltiTools-Economy` again, and read the log | Step 1: a line containing `Module 'UltiTools-Economy' refused to load: InterestService.payInterestIfEnabled is bound to EconomyConfig key 'interest.interval'` that also contains `which has value 0` and `0 does not mean off`; `/money` is not a known command. Step 2 (control, proving the refusal came from the value): the module loads and logs its `Registered config-bound @Scheduled task: InterestService.payInterestIfEnabled` line. Step 3: one WARN line containing `UltiTools-Economy: InterestService.payInterestIfEnabled is bound to EconomyConfig key 'interest.interval', which has value 0 after the reload` that ends `keeping 1800s`, and no `rescheduled config-bound @Scheduled task` line — the running 1800-second interval is kept. Step 4: no `rescheduled config-bound @Scheduled task InterestService.payInterestIfEnabled` line and no WARNING naming `interest.interval` — the running value was already 1800. This is the observable that would differ if step 3 had changed the value: restoring 1800 would then be a change and would log `rescheduled config-bound @Scheduled task InterestService.payInterestIfEnabled after reload (delay=36000, period=36000, async=false)`. `ultieconomy.scheduled.interest-payment.reload-interval` above shows that line does appear on a real change. The file is back to a value that loads, which the later rows that restart the server rely on (UltiKits/UltiEconomy#15, UltiKits/UltiTools-Reborn#531) | server | |
| ultieconomy.scheduled.leaderboard-refresh | PlaceholderAPI installed; two players A and B who have both joined before and are both online; A's total wealth (cash + bank) is the largest of every account on the server and B's the second largest (e.g. `/eco set A 1000000` and `/eco set B 500000`, both with bank `0.00`), set before the restart; `leaderboard.update-interval: 60` in the on-disk `config.yml`; `interest.interval` and `leaderboard.update-interval` in the on-disk `config.yml` hold valid values (whole seconds from 1 to 107374182; an invalid one refuses the whole module at load) | 1. Restart the server and read this boot's log. 2. As soon as A has joined, run `/papi parse me %ultieconomy_top_name_1%` and `/papi parse me %ultieconomy_rank%` as A. 3. From console run `/eco set B 5000000`, then wait 65 seconds. 4. Run `/papi parse me %ultieconomy_top_name_1%` and `/papi parse me %ultieconomy_rank%` as A again | Step 1: exactly one line containing `Registered config-bound @Scheduled task: LeaderboardService.refreshAll (delay=0, period=1200, async=false, config=EconomyConfig, periodKey=leaderboard.update-interval=60s)`. Step 2: `A`'s own name and `1` — the leaderboard was filled at load, not left empty. Step 4: `B`'s name and `2` — the refresh picked up the change within 60 seconds (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.scheduled.leaderboard-refresh.reload-interval | `ultieconomy.scheduled.leaderboard-refresh` above passed; PlaceholderAPI installed; two players A and B who have both joined before and are both online; before the restart, from console, `/eco set A 1000000` and `/eco set B 500000`, both with bank `0.00`, so that A's total wealth (cash + bank) is the largest of every account on the server and B's is below it (the row above leaves B at `5000000`, above A, so this reset is required); in the on-disk `config.yml`, `leaderboard.update-interval: 20` (NOT the shipped `60`: this row lengthens the interval, so the reload always lands partway through a cycle), and `interest.interval` holds a valid value (whole seconds from 1 to 107374182; an invalid one refuses the whole module at load) | 1. Restart the server and read this boot's log. 2. As soon as A has joined, run `/papi parse me %ultieconomy_top_name_1%` as A. 3. At any moment after step 2, set `leaderboard.update-interval: 60` in the same file, run `/ul reload UltiTools-Economy` from console, note the time R of the reload, and read the log. 4. Within 10 seconds of R, run `/eco set B 9000000` from console. At R+35 seconds, run `/papi parse me %ultieconomy_top_name_1%` as A. 5. At R+65 seconds, run it again as A (below 20 TPS, repeat at R+90 seconds before judging). The file and the running interval are now both back at the shipped `60`, so no restore step is needed | Step 1: exactly one line containing `Registered config-bound @Scheduled task: LeaderboardService.refreshAll (delay=0, period=400, async=false, config=EconomyConfig, periodKey=leaderboard.update-interval=20s)`. Step 2 (control, proving B is not already first): `A`'s name. Step 3: exactly one `rescheduled config-bound @Scheduled task` line, containing `UltiTools-Economy: rescheduled config-bound @Scheduled task LeaderboardService.refreshAll after reload (delay=0, period=1200, async=false); next run in`. It is followed by a number of ticks from 800 to 1199, and there is no new `Registered config-bound @Scheduled task` line. The window holds wherever the reload lands: at the old 20-second interval, the last refresh ran fewer than 400 ticks before, so keeping the cycle's place gives the new 1200 minus the ticks already elapsed. Failure values: exactly `1200` means the reload restarted the clock from the reload. (In 1 run out of 400, a correct reload lands on the refresh's own tick and also reads `1200`; if it does, repeat steps 1-3 once before judging.) `1` means the reload ran the refresh at once. A number below 800 cannot come from the 20-second cycle. No line means the new value was not applied. Step 4: `A`'s name. The next refresh comes at least 800 ticks after R, which is 40 seconds at 20 TPS and later below it, so no refresh has run since the `/eco set`. `B`'s name here means the old 20-second cycle was still running, because it refreshes within 400 ticks, which is 25 seconds at 16 TPS. Below 16 TPS this step cannot tell, and the row is judged on steps 3 and 5. Step 5: `B`'s name. The refresh at most 1199 ticks after R, under 60 seconds at 20 TPS, picked up the change (UltiKits/UltiEconomy#15, UltiKits/UltiTools-Reborn#531) | server | |

## Leaderboard placeholders

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.placeholder.bank | PlaceholderAPI installed; `ultieconomy.placeholder.register` above passed | Run `/papi parse me %ultieconomy_bank%` | Resolves to the player's own bank balance as a raw two-decimal number | server | |
| ultieconomy.placeholder.cash | PlaceholderAPI installed | Run `/papi parse me %ultieconomy_cash%` | Resolves to the player's own cash balance as a raw two-decimal number | server | |
| ultieconomy.placeholder.cash-formatted | PlaceholderAPI installed | Run `/papi parse me %ultieconomy_cash_formatted%` | Resolves to the player's cash balance WITH the configured currency symbol prefixed (e.g. `$1,000.00`), unlike the raw-number rows above | server | |
| ultieconomy.placeholder.currency-scoped | PlaceholderAPI installed; a second, non-primary currency (e.g. `gems`) configured; the player has joined at least once since `gems` was configured (so a `gems` `CurrencyBalanceEntity` row exists) and holds a nonzero, distinguishable `gems` bank balance (so `bank`/`total` differ visibly from `cash` alone), their `gems` total being the largest `gems` total on the server, unchanged for at least the last 65 seconds; `leaderboard.update-interval: 60` in the on-disk `config.yml` | Run `/papi parse me %ultieconomy_gems_cash%`, then separately `%ultieconomy_gems_bank%`, `%ultieconomy_gems_total%`, `%ultieconomy_gems_cash_formatted%`, and `%ultieconomy_gems_rank%` — this row's own `FEATURES.md` entry bundles all five `handleCurrencyPlaceholder` branches into one row specifically because they share identical dispatch logic, so exercising only `cash` and calling the row complete would let a regression in any of the other four pass unnoticed | `gems_cash` and `gems_bank` each resolve to their own distinct raw two-decimal number (not to the primary currency's own `%ultieconomy_cash%`/`%ultieconomy_bank%` values); `gems_total` resolves to their sum; `gems_cash_formatted` resolves to the same cash figure WITH `gems`'s own symbol prefixed; `gems_rank` resolves to `1` — the player's rank in the `gems` leaderboard `ultieconomy.scheduled.leaderboard-refresh` rebuilds, not `-` (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.placeholder.rank | PlaceholderAPI installed; two players online whose total wealth (cash + bank) is the largest and second-largest of every account on the server (e.g. `/eco set` to `1000000` and `500000`, bank `0.00`), unchanged for at least the last 65 seconds; `leaderboard.update-interval: 60` in the on-disk `config.yml` | Run `/papi parse me %ultieconomy_rank%` for BOTH players | The wealthier player sees `1` and the other sees `2` — ranks from the leaderboard `ultieconomy.scheduled.leaderboard-refresh` rebuilds every `leaderboard.update-interval` seconds (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.placeholder.top-balance | PlaceholderAPI installed; one player whose total wealth is the largest of every account on the server, set to exactly `1000000.00` (e.g. `/eco set <player> 1000000` with bank `0.00`) at least 65 seconds ago; `leaderboard.update-interval: 60` in the on-disk `config.yml` | Run `/papi parse me %ultieconomy_top_balance_1%` | Resolves to `1000000.00` (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.placeholder.top-name | PlaceholderAPI installed; one player online whose total wealth is the largest of every account on the server, unchanged for at least the last 65 seconds; `leaderboard.update-interval: 60` in the on-disk `config.yml` | Run `/papi parse me %ultieconomy_top_name_1%` | Resolves to that player's name (UltiKits/UltiEconomy#15) | server | |
| ultieconomy.placeholder.total | PlaceholderAPI installed | Run `/papi parse me %ultieconomy_total%` | Resolves to the player's cash + bank balance as a raw two-decimal number, matching the numeric value of `/money`'s `Total wealth: ...` line | server | |

## Data persistence

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.persistence.balance-restart | A player's primary-currency cash/bank changed via any command above (e.g. `ultieconomy.deposit.deposit`), and a per-currency balance changed via a `-currency` command above, both noted before the restart; `interest.interval` and `leaderboard.update-interval` in the on-disk `config.yml` hold valid values (whole seconds from 1 to 107374182; an invalid one refuses the whole module at load) | Stop the server completely (not `/ul reload`), start it again, then run `/money` and `/money gems` for the same player | Both the primary-currency and the per-currency balances read back EXACTLY the values noted before the restart, not the pre-change defaults | server | |
| ultieconomy.persistence.treasury-restart | A nonzero treasury balance from a collected transaction tax, noted via `/eco treasury` before the restart; `interest.interval` and `leaderboard.update-interval` in the on-disk `config.yml` hold valid values (whole seconds from 1 to 107374182; an invalid one refuses the whole module at load) | Stop the server completely, start it again, then run `/eco treasury` | The treasury balance reads back exactly the value noted before the restart | server | |

## Configuration

One row per shipped yml file (D-06's config-per-file rule, narrowed for this module to exactly
one row per this plan's own instruction — see the Conventions block above for why
`currencies.yml` is folded into this same row rather than given its own).

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.config.config-yml | Fresh `plugins/UltiTools/pluginConfig/UltiTools-Economy/config/config.yml` AND `.../config/currencies.yml` at their shipped defaults (not hand-edited; `UltiTools-Economy` is this module's own `plugin.yml` `name:`, which `UltiToolsPlugin#getPluginName()` reads to build its `pluginConfig` subdirectory — NOT `UltiEconomy`, which does not correspond to any directory this module's own code creates) — server started at least once | Confirm the 12 keys the packaged resource ships (`initial-cash`, `currency-name`, `currency-symbol`, `bank.*`, `interest.*`, `leaderboard.*`) are present in the on-disk `config.yml` at their documented defaults, AND that the 7 `tax.*` keys — absent from the packaged resource — have been migrated onto disk with their Java-declared defaults after this first boot (per `AbstractConfigEntity#init()`); confirm `currencies.yml` carries exactly the one shipped `coins` currency marked `primary: true`; then set `language: "en"` and restart, confirming `/money`'s header switches from the localized (untranslated at `MoneyCommand.java:63`'s Chinese key) block to `Economy System` (the one working header line, proving the config file itself loads and applies); separately, add a second currency `gems` to `currencies.yml` (not marking it `primary`), restart, and confirm `/money gems` now resolves without the "currency not found" refusal it would have produced before the edit — proving `currencies.yml`'s own content genuinely drives `CurrencyManager`'s resolution set, not merely that the file is well-formed; as a THIRD, deliberately-invalid edit within this same row, mark BOTH `coins` and `gems` `primary: true` at once and restart again | All 12 packaged `config.yml` keys present at their documented defaults before either change (`interest.enabled: false`, `interest.interval: 1800`, `leaderboard.update-interval: 60`), plus all 7 `tax.*` keys present at their Java-declared defaults after the first boot; `currencies.yml` starts with exactly the one `coins` entry; after the language change, `/money`'s header reads `Economy System`; after adding `gems`, `/money gems` resolves instead of refusing; after marking two currencies `primary: true` at once, the module FAILS to load — `CurrencyManager`'s constructor throws `IllegalStateException` ("Multiple primary currencies: ...") during `UltiEconomy#getCurrencyManager()`'s first call, which the framework's `PluginManager` catches and reports as a failed module registration, not a graceful in-game refusal. Do NOT attempt to observe an effect from `tax.wealth-tax.*` — it has none (UltiKits/UltiEconomy#27); `tax.enabled` does, and is exercised by `ultieconomy.pay.pay.neg-tax-master-off` | server | |

## Language

| ID | Preconditions | Steps | Expected | Layer | Covers |
|---|---|---|---|---|---|
| ultieconomy.i18n.language | `language: en` in the framework's `plugins/UltiTools/config.yml`, set once for the session before the start; before the start, `plugins/UltiTools/pluginConfig/UltiTools-Economy/lang/` was moved into the session backup directory so this jar's catalogues are extracted fresh (an upgraded install never refreshes an already-extracted language file, UltiKits/UltiTools-Reborn#459); `bank.enabled: true`; the tester online | The tester runs `/money`, then `/bank help`; from the console run `/eco check <tester>` | `/money` shows exactly `=== Economy System ===`, `Your balance: <cash>`, `Your bank balance: <bank>`, `Total wealth: <total>`; `/bank help` shows `=== UltiEconomy Bank ===`, `/bank - View bank balance`, `/bank <currency> - View bank balance (specific currency)`; the console shows `=== <tester> ===`, `<tester>'s balance: <cash>`, `<tester>'s bank balance: <bank>`, `Total wealth: <total>`; none of the lines these commands printed contains Chinese text (UltiKits/UltiEconomy#14) | server | |
